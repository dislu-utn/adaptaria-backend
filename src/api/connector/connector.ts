/*
function connector_sync(entity: string, entity_id: string, method: string): Promise<void> {
  // Función abstracta, no hace nada por ahora
  return Promise.resolve();
}

function connector_handshake();
*/
